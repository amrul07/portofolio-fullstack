"use client";
import React from "react";
import Typography from "@mui/material/Typography";
// import styled from "@emotion/styled";
import { styled } from "@mui/material/styles";

export const Poppins = styled(Typography)(({ theme }) => ({
  fontFamily: "Poppins",
  fontSize: "1.1rem",
  // lineHeight: "2rem",
}));
